# Convenience pin list. pyproject.toml is the source of truth for installs.
# Use: pip install -e .   (or: pip install -e ".[dev]")
docling>=2.0.0
pdfplumber>=0.11.0
pypdf>=4.0.0
sentence-transformers>=3.0.0
chromadb>=1.0.0
torch>=2.0.0
tqdm>=4.66.0
rich>=13.0.0
typer>=0.12.0
keyring>=24.0.0
